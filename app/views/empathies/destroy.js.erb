$(".question_<%= @question.id %>").html("<%= j( render 'questions/show', question: @question, question_user_url: @question_user_url ) %>")

$(".questions_<%= @question.id %>").html("<%= j( render 'questions/question', question: @question, image_url: @image_url ) %>")
