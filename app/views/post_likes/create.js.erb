if (location.pathname == '/post') {
  $(".post_<%= @post.id %>").html("<%= j( render 'posts/show',  post: @post ) %>")
} else {
  $(".post_<%= @post.id %>").html("<%= j( render 'posts/post',  post: @post ) %>")
}