<div class="container">
	<div class="row">
		<div class="col-md-9 col-sm-12">
		  <h2>Follower一覧</h2>
		  <% if @users.count > 0 %>
		    <%= render 'users/index', users: @users %>
		  <% else %>
		    <p>ユーザーはいません</p>
		  <% end %>
    </div>
  </div>
</div>